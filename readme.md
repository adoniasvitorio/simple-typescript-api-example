# Simple Typescript api example


## Some Notes

**Typescript config**

 ```bash
yarn tsc --init
 ```

 *To separate JS files in project I found this solution: in tsconfig.json uncomment line with **"outDir": "./"** and change by **"outDir": "./dist"**,*

  ```bash
yarn tsc
 ```